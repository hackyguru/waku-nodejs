import { ConnectionManager } from "@waku/core";
import { type IFilter, type ILightPush, type Libp2p, type ProtocolCreateOptions } from "@waku/interfaces";
export declare function wakuFilter(connectionManager: ConnectionManager, lightPush?: ILightPush, init?: ProtocolCreateOptions): (libp2p: Libp2p) => IFilter;
